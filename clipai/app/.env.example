# Copy this to app/.env.local for development
# Set VITE_API_URL to your Railway worker URL in production

# Development (local worker)
VITE_API_URL=http://localhost:8000

# Production example:
# VITE_API_URL=https://clipai-worker.railway.app
