flask==3.0.3
flask-cors==4.0.1
boto3==1.34.120
google-generativeai==0.8.3
groq==0.9.0
yt-dlp==2024.5.27
requests==2.32.3
python-dotenv==1.0.1
gunicorn==22.0.0
# v3 — persistence, auth, plan-gating
supabase==2.7.4
redis==5.0.7
psycopg2-binary==2.9.9
APScheduler==3.10.4
PyJWT==2.9.0
# v4.3 — multi-platform trend sources (keyless where possible)
pytrends==4.9.2
# Reddit uses public .json endpoints — NO praw, NO API key, NO policy acceptance.
# Twitch Helix API dropped (requires 2FA-locked dev console).
